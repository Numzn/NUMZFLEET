{
  "name": "numzfleet-release-control-center",
  "version": "1.0.0",
  "private": true,
  "description": "NUMZFLEET Release Control Center — orchestration layer for Release Pipeline v3",
  "scripts": {
    "dev": "concurrently -n api,ui -c blue,green \"npm run dev:api\" \"npm run dev:ui\"",
    "dev:api": "npm run dev --prefix api",
    "dev:ui": "npm run dev --prefix ui",
    "build": "npm run build --prefix ui",
    "start": "npm run start --prefix api",
    "collect": "npm run collect --prefix api"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
